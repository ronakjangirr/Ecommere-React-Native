import { usePathname } from "expo-router";
import {
  View,
  StyleSheet,
} from "react-native";
import Register from "./register";

function Index() {

  return (
      <Register/>
  );
}

export default Index;
